// Generated by CMake from cmake/openads_version.h.in -- do not edit.
// configure_file() is copy-if-different: this header's timestamp only
// bumps when the version string actually changes, so after a commit only
// the handful of TUs that include it recompile instead of the whole tree
// (the previous global add_compile_definitions(OPENADS_VERSION_STR=...)
// changed the command line of every TU on every configure).
#define OPENADS_VERSION_STR "@OPENADS_VERSION_STR@"
